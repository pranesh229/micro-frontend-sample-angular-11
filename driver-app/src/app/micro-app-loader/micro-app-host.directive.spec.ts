import { MicroAppHostDirective } from './micro-app-host.directive';

describe('MicroAppHostDirective', () => {
  it('should create an instance', () => {
    const directive = new MicroAppHostDirective();
    expect(directive).toBeTruthy();
  });
});
